import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.losalpes.muebleria",
  appName: "Los Alpes",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
